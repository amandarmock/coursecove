/**
 * E2E Test Fixtures
 *
 * Re-exports all fixtures for convenient importing in tests.
 *
 * Usage:
 *   import { test, expect } from '../../fixtures';
 */

export { test, expect, TEST_USERS } from './auth.fixture';
export type { TestUser } from './auth.fixture';
